# JavaScript 学习进度

最后更新：2026-08-13

## 已确认掌握

- 变量：`let`、`const` 的基本用途。
- 基本数据类型：字符串、数字、布尔值。
- 常用运算符与条件判断：比较运算符、`if / else if`；了解 `==` 与 `===` 的基本区别。
- 普通 `for` 循环：`index`、`index++`、数组下标和 `.length`。
- 函数：形参、实参、`return` 和模板字符串。
- 数组基础：用下标读取元素；理解下标从 `0` 开始、长度从 `1` 计数。
- 数组方法：能使用 `push()` 在数组末尾添加一项，并在 Astro 前置脚本区通过 Node.js 终端观察结果。
- 对象基础：用点语法读取和修改属性，例如 `post.title`、`post.views`。

## 尚未系统学习

- 数组方法：`pop`（已自行尝试，待正式理解）、`includes`、`map`、`filter`、`find`。
- 对象进阶：嵌套对象、`Object.keys`。
- 字符串方法：`split`、`trim`、`toLowerCase`。
- 页面交互：DOM、点击事件、读取输入框内容。
- 模块：`import / export`。
- 异步：`Promise`、`async / await`、`fetch`。

## 当前学习策略

后续开始学习前，先读取本文件，再只安排一个与博客有关的小主题；不重复测试已经确认掌握的基础内容。

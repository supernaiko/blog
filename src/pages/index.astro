---
console.log("欢迎使用");
import './styles/global.css';
import Navigation from '../components/Navigation.astro';
const postTitle = ["欢迎使用Astro","2","Astro是一个现代的静态站点生成器"];
postTitle.push("这是一个新的标题");
console.log(postTitle);
postTitle.pop();
console.log(postTitle);
console.log(postTitle.includes("欢迎使用Astro"));
console.log(postTitle.includes("1"));
let c = []
for(let i = 0; i< postTitle.length; i++) {
	c.push({"文章": postTitle[i]})}
console.log(c);

const makePostMessage = (title:string, index:number) => {
	return `文章标题: ${title} (索引: ${index})`;
};
console.log(makePostMessage("JavaScript 数组方法", 0));
console.log('-----------------------------------------------------');
const post = postTitle.map((title:string, index:number) => {
	return `文章标题: ${title} (索引: ${index})`;
});
console.log(post);
const postMessages = postTitle.forEach((title:string)=>{
	 console.log(`文章1111${title}`);
})
console.log(postMessages);
const postStats ={a:1,b:2,c:3};
let postStatEntries  = Object.entries(postStats);
console.log(postStatEntries);
const rawTag =  " javascript,  html, css, astro, react, vue, svelte  ";
const cleanTag = rawTag.trim();
console.log(`[${rawTag}]`);
console.log(`[${cleanTag}]`);
console.log('26-8-17================================================================');
const tags = cleanTag.split(',').map(tag => tag.trim());
console.log(tags);


---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Astro</title>
	</head>
	<body>
		<Navigation />
		<h1>站点</h1>
		
	</body>
</html>
